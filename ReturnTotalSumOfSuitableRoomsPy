def solution(matrix):
  r = len(matri)
  c = len(matrix[0])
  total = 0
  for j in range(c):
    for i in range(r):
      if matrix[i][j]!=0;
        total+=matrix[i][j]
      else:
        break
  return total
